# Write a Python program that will accept the base and height of a triangle and compute the area

print("Calculate area of a triangle")
base = float(input("Give a base: "))
height = float(input("Give a height: "))

print(base*height/2)